name: diffrax
channels:
  - conda-forge
  - defaults
dependencies:
  - pip        
  - pip:
      - wandb
      - matplotlib
      - jax
      - jaxlib
      - optax
      - equinox
      - diffrax
      - fire
prefix: /opt/anaconda3/envs/diffrax
